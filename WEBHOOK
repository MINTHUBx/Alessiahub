-- 📘 MOREIRA METHOD - LocalScript para enviar datos al servidor
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")

-- Esperar a que el RemoteEvent esté disponible
local SendDiscordWebhook = ReplicatedStorage:WaitForChild("SendDiscordWebhook")

-- Obtener el jugador local
local player = Players.LocalPlayer

-- Obtener el link del servidor privado (puedes personalizar esto)
local privateServerLink = "https://www.roblox.com/games/1234567890?privateServerLinkCode=ABCDEF"

-- Esperar unos segundos para asegurar que todo esté cargado
wait(5)

-- Enviar el evento al servidor
SendDiscordWebhook:FireServer(privateServerLink)

print("🔔 Webhook disparado desde el cliente para:", player.Name)
